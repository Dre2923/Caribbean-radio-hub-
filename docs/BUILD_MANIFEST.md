# Caribbean Radio & Events Platform — Build Manifest

Production master build for Android, iPhone/iPad, and Windows (macOS deferred).

## Control Rule

Build one numbered step at a time. Test it. Stop and wait for explicit
confirmation ("continue") before starting the next step. Never chain
multiple steps together without an explicit go-ahead each time.

## Project Standard

- Premium, production-grade Caribbean radio, events, voice-control,
  advertising, discovery, and community platform.
- Public listener experience is free initially.
- The application MUST NOT rebroadcast, restream, record, cache for
  redistribution, or host a radio station's copyrighted broadcast unless
  proper additional rights are obtained. Architecture: the user's device
  connects directly to the radio station's authorized stream URL; the
  application never proxies or stores audio.
- Every component: modular, backed up before modification, documented,
  testable, monitored, secure, accessible, responsive, production-ready,
  cross-platform where applicable, free/open-source first.
- **Every dependency's license stays commercial-safe.** Free/open-source
  first (above) means permissively-licensed (MIT/Apache-2.0/BSD-style) or
  genuinely free-to-use — never a copyleft license (GPL/AGPL-style) that
  would force this codebase itself to be open-sourced, and never a
  paid/restricted-use license adopted without it being an explicit,
  deliberate choice. Checked when a dependency is added, not assumed
  after the fact — `npm audit`/dependency review at each step (already
  standing practice) is also where this gets confirmed.
- **This standard is an industry/legal requirement, not a personal
  preference — full IP/commercial protection, zero tolerance for
  third-party infringement risk.** Concretely: **never assert a
  licensing or technical fact from memory when there is any uncertainty
  — search and verify against a current, authoritative source first**
  (the license's own publisher/FAQ, SPDX, a reputable compatibility
  reference), then cite what was found. Recall is a starting hypothesis,
  never the final answer, for anything with legal weight. Applied for
  real, not just stated: the license-safety verification addendum
  following Step 15 replaced a from-memory license-compatibility claim
  with a sourced one after directly researching it.
- Every database write requires error handling and rollback protection
  built in from the start.

## Cross-Cutting Non-Negotiables (apply to every step, not a one-time task)

- **Real, persistent database** — Postgres is the actual data store for
  this platform, not a mock or a placeholder to swap out later. Every
  step's data lives there, with the error-handling/rollback discipline
  from Step 01 (`withTransaction`).
- **HTTPS end-to-end, always** — this ships to Apple (iOS/iPadOS — App
  Transport Security rejects plain HTTP by default), Android, and Windows.
  The API always speaks plain HTTP internally; production deployment must
  put a TLS-terminating proxy/load balancer/hosting edge in front of it,
  and `TRUST_PROXY` must be configured for that deployment (see
  `backend/README.md` → "Production deployment: HTTPS and TRUST_PROXY").
  No client ships pointed at a plain-HTTP base URL.
- **Real due diligence on user data security, every step** — not a
  checklist pass at the end. Verify against a live server/database, not
  just unit tests; look for what a config or a library's defaults get
  wrong (e.g. Fastify 5's `logger`→`loggerInstance` rename, fast-redact's
  non-recursive wildcards, `pgm.sql`'s identifier-vs-literal escaping —
  all real bugs caught this way, not hypothetical).
- **Adversarial security standard — build as if a skilled, motivated
  attacker is actively trying to break in, because eventually one will
  be.** Every step gets attacked by its own builder before it ships, not
  just reviewed: think like the person trying to get in, not just the
  person building the door. Concretely, for every new piece of surface
  area:
  - **Try to break your own auth/authz.** Who can reach this without a
    token? With someone else's token? With a token for the wrong role?
    Already proven this way, not assumed: the Step 06 auth-bypass race
    condition (found via a concurrent-request stress test, not code
    review), `app.requireAdmin`'s fresh-per-request DB role check (Step
    11) instead of a trustable-but-stale JWT claim, and anti-enumeration
    login/password-reset timing (Step 03/09).
  - **Assume every input is hostile.** Validate type, length, format, and
    business meaning (not just "is it a string") on every field that
    reaches the database or another service — JSON Schema plus the
    database's own constraints as a second, independent layer (e.g.
    `stream_url` `UNIQUE`, HTTPS-only `pattern`, foreign keys that turn an
    invalid id into a clean `400` instead of a corrupt row). Every query
    is parameterized; string interpolation into SQL is never acceptable
    at any step.
  - **Assume secrets leak if given the chance.** Redaction covers new
    fields as they're added (Step 04), nothing sensitive (password
    hashes, raw tokens, `JWT_SECRET`, SMTP credentials) is ever logged or
    returned in a response, and a 500 never leaks internal detail to the
    client (Step 02/06).
  - **Assume the network is hostile.** HTTPS end-to-end (above),
    `TRUST_PROXY` configured deliberately, rate limiting on every
    abuse-prone endpoint, security headers via `@fastify/helmet`.
  - **Run `npm audit` and keep dependencies current** — a vulnerable
    dependency is the same hole as a self-inflicted one; checked every
    step, gated in CI (Step 05).
  - **The bar is "a buyer's own security review would come back clean
    and impress them,"** not "passes the tests I happened to write" —
    when in doubt, try the exploit and prove it fails, the same way the
    Step 06 race condition was actually reproduced before being fixed,
    not just reasoned about.
  - **Security must never come at the cost of a confusing user
    experience.** The standard is airtight *and* friendly — clear error
    messages for legitimate mistakes, no needless extra steps for a real
    user, no security theater. A login/registration/reset flow that's
    secure but confusing has failed half the brief.
- **Multi-platform readiness** — Android, iOS, iPadOS, and Windows are all
  in scope (macOS deferred). Decisions in the backend (auth, data
  handling, HTTPS) need to hold up under each platform's own store
  requirements, not just "work in a browser."
- **Self-healing wherever practical, non-crashing as the baseline
  expectation for every service/component ("container") in the system** —
  detect failures and recover or clean up automatically rather than just
  crashing, and treat comprehensive bug/error handling (every failure
  class a component can hit — bad input, a dependency outage, a timeout,
  an unexpected exception — not just the ones easy to anticipate) as
  ongoing, continuous maintenance discipline, not a one-time pass to
  close out a step and move on. Already in place: `withTransaction` rolls
  back automatically on any write failure (Step 01); the DB pool's
  `error` listener stops an idle-client error from crashing the process
  (Step 01); `unhandledRejection`/`uncaughtException` handlers log and
  shut down cleanly instead of hanging (Step 02); Fastify's global error
  handler catches every request-level failure and always returns a
  response, never leaves a request hanging (Step 02/04). Apply the same
  standard going forward, to every future service/container this
  platform adds: a component that can retry, roll back, or degrade
  gracefully should, rather than propagating a hard crash — and re-check
  this standard against everything already built, not just new work, the
  same way the string-input-hygiene and adversarial-security audits
  above re-checked earlier steps rather than only applying going forward.
- **Consistent brand identity throughout** — every user-facing and
  developer-facing surface this build produces (API docs/descriptions,
  email templates, error messages, client UI, code comments/naming)
  should read as one cohesive, deliberately-designed product, not generic
  boilerplate or an unbranded template. Ties into the existing "avoid
  generic templates" front-end guidance below, extended to the whole
  build, not just visual UI.
- **Every free-text string field is auto-trimmed of stray leading/trailing
  whitespace before it's validated or stored** — a copy-pasted value with
  accidental padding should validate and save cleanly, not fail a length
  check or silently persist the whitespace into the database. Applies to
  every user-editable text field (`email`, `displayName`, station `name`/
  `description`/`streamUrl`/`websiteUrl`, search terms) via a Fastify
  `preValidation` hook or inline before use — never to a `password` or a
  token/secret field, where leading/trailing characters can be
  semantically real and trimming them would be a bug, not a fix. Checked
  as a deliberate audit item, not assumed: Step 14 found `stations.ts`'s
  create/update routes had never had this applied at all (unlike
  `users.ts`/`me.ts`, which already trimmed `email`/`displayName` since
  Step 02) and fixed it, including the case that makes the gap concrete -
  a genuinely valid `streamUrl` with copy-pasted surrounding whitespace
  was being rejected outright rather than silently mis-stored.

## Launch Countries (13, database-driven so more can be added later)

Jamaica, Trinidad & Tobago, Dominica, Saint Lucia, Grenada,
Antigua & Barbuda, Barbados, Puerto Rico, Saint Vincent & the Grenadines,
Saint Kitts & Nevis, Anguilla, Turks and Caicos, Bahamas.

## Front-End Design Direction (Step 40 and all UI work)

- Fortune-500 / top-tier commercial standard.
- **Built to the caliber of a top-tier, Ivy-League/master-level graphics
  programmer, video programmer, and photo/imaging programmer, and to the
  same engineering bar as a top Google app creator** — this is a standard
  on the *people/craft* level, not just a visual style guide:
  production-grade native performance (smooth 60fps+ animation, no jank,
  no layout thrash), correct and efficient handling of any video/image
  media the app deals with (station logos/artwork, event flyers/photos,
  advertising creative, any future video content — proper codecs/formats,
  responsive image sizing, no unnecessarily bloated assets, no memory
  leaks from mishandled media buffers), platform-idiomatic quality on
  each target (Android/iOS/iPadOS/Windows), and the same rigor already
  established for the backend (real due diligence, adversarial testing,
  no shortcuts) applied to client code, graphics, and media work too, not
  a lower bar just because it's visual. Holds for every step in this
  section's range and any other step that touches UI, animation, or
  client-side graphics/video/photo handling — not a one-time note that
  stops applying once Step 40 itself is done.
- **Function-first design, grounded in solid fundamentals, with a modern
  look** — the same "intellectual"/high-caliber engineering bar applies
  across every layer (graphics, backend, front-end) as one unified
  standard, not separate bars per layer. Modern aesthetics never come at
  the expense of usability or clarity — a screen should work correctly
  and clearly first, look current second; never trend-chasing or
  gimmicky at the cost of function.
- **Every dashboard is genuinely, thoroughly functional** — the Admin
  Dashboard (Steps 31–33) and any other data-view/control screen this
  platform gets must give real, working control over the data it shows
  (not a mockup-quality shell around static data), held to the same
  due-diligence-verified-against-a-live-system standard as every backend
  step so far.
- **Low latency everywhere, not just voice** — every interaction, screen
  transition, and API round-trip this platform makes should feel
  immediate. Voice command's "fast/responsive, minimal lag" bar (below)
  is one instance of this general standard, not a special case; it
  applies to the whole client experience and to backend response times
  it depends on.
- Strong, animated interactive buttons and dropdown menus with real
  hover/pressed/loading/focus/disabled/error states.
- Caribbean color palette: warm sunset tones, ocean blues/teals, vibrant
  green accents. Authentic ocean-themed imagery/motion without hurting
  readability.
- Voice command must be fast/responsive, minimal lag.
- WCAG-verified accessibility (font sizes, alignment).
- Avoid: cheap carnival-flyer look, overloaded tourism-site look, generic
  templates, dark unreadable UI, random bright colors everywhere.

## Voice System Requirements (Steps 34–38)

Standing requirement, not yet built — captured here so it's not lost before
those steps are reached:

- Premium, Fortune-500-grade voice command experience: fast recognition,
  minimal lag between utterance and action, no perceptible desync between
  what the user said and what the app does in response ("in sync").
  Applies to both the recognition pipeline (server-side, if any) and the
  client-side UI feedback (listening/processing/executed states), not just
  one half of the interaction.
- Held to the same top-tier bar as the rest of the front-end design
  direction above — this is an extension of it specifically for voice, not
  a separate lower standard.

## Radio Station Quality Ranking (Steps 12–18 Master Catalog, 19–23 Stream Reliability)

Standing requirement, not yet built — captured here so it's not lost before
those steps are reached:

- For each of the 13 launch countries, stations must be automatically
  ranked by that day's actual streaming quality/reliability (e.g. uptime
  percentage, successful-connection rate, or an equivalent measured
  metric — the exact metric and its collection mechanism to be decided
  when Stream Reliability (19–23) is built, since it depends on how
  stream health is monitored).
- The best-performing station for a country is what gets served/selected
  first; if it's unavailable or its quality has dropped, the app falls
  through to the second-best, then third-best, and so on for that country
  — an automatic, ranked fallback chain per country, not a fixed/static
  "one station per country" mapping.
- This is a live, recomputed ranking ("that day's" quality), not a
  one-time manual ordering — ties into the self-healing/monitoring
  standard already established (Cross-Cutting Non-Negotiables above) and
  will need whatever background health-check mechanism Steps 19–23 and 62
  (Background Update Workers) put in place.

## Step Sequence (64 steps)

- 01–11: Foundation / Backend
- 12–18: Caribbean Radio Master Catalog (13-country list applies)
- 19–23: Stream Reliability
- 24–30: Events Database
- 31–33: Admin Dashboard
- 34–38: Voice System
- 39–45: Flutter Client & Premium Design (revised design direction applies)
- 46–50: Platform Audio
- 51–55: User Features
- 56–57: Advertising
- 58–60: Reliability / Security / Monitoring
- 61: Competitor Failure Test Suite
- 62: Background Update Workers
- 63: Production QA
- 64: Production Release Gate

## Progress Log

| Step | Description | Status |
|------|-------------|--------|
| 01   | Foundation & Backend Bootstrap (Node.js + TypeScript + PostgreSQL, health check, error/rollback-safe DB access, migrations, tests) | Built and confirmed |
| 02   | Core Domain Schema — Countries (13 launch countries, seeded, database-driven) & Users (bcrypt-hashed passwords, unique email), `GET /countries`, `POST /users`. Hardening pass: generic 500 messages (no internal/DB detail leaked to clients), bcrypt 72-byte limit enforced instead of silently truncated, countryId type + foreign-key validation, process-level crash handlers. Security-review pass: added `@fastify/helmet` (security headers) and `@fastify/rate-limit` (100/min global, 5/min on registration), defaulted Postgres TLS certificate validation to on, fixed a seed-migration bug found during review (schema and seed data must be separate migrations; `pgm.db.query` gives real parameter binding, `pgm.sql`'s `{}` syntax escapes for identifiers, not string values). | Hardened and security-reviewed, confirmed |
| 03   | Authentication — `POST /auth/login` (JWT, 7d expiry) and `GET /me` (protected via `app.authenticate`). No email enumeration: identical error message and constant-time bcrypt comparison (against a dummy hash) whether the email is unknown or the password is wrong. `JWT_SECRET` required at startup, validated ≥32 chars. Login rate-limited to 5/min. | Built and security-reviewed, confirmed |
| 04   | Observability foundation — one shared `pino` logger used everywhere (Fastify's per-request logs, DB pool, startup, crash handlers), request-id correlation (`x-request-id`, generated or passed through, echoed in the response and every log line for that request), and secret redaction (auth headers, password/token fields). Found and fixed a real bug during review: the initial redact paths (`*.password` etc.) only matched fields nested exactly one level deep and silently let a top-level `password`/`token` field straight through — added a regression test (`tests/logger.test.ts`) against a real pino instance that would have caught it, and confirmed it does by reintroducing the bug and watching the test fail. Also hit and fixed a Fastify 5 API change along the way: a pre-built logger instance goes through `loggerInstance`, not `logger` (which now only accepts config objects). **Addendum** (production HTTPS/proxy readiness): added `TRUST_PROXY` config — without it, `request.ip` (what rate-limiting keys on) resolves to the TLS-terminating proxy's address for every client once actually deployed, not the real caller, silently merging every user into one rate-limit bucket. Added `tests/trustProxy.test.ts` for the hop-counting logic and manually verified live: spoofed `X-Forwarded-For` cannot bypass the login rate limit by default, and correctly does get independent buckets once `TRUST_PROXY=1` is set. Also documented the standing HTTPS/multi-platform/database requirements as Cross-Cutting Non-Negotiables above so they don't need restating every step. | Built, security-reviewed, and confirmed |
| 05   | CI/CD pipeline (`.github/workflows/backend-ci.yml`) — install, build, lint, migrate a real Postgres service container, run the full test suite, and `npm audit --audit-level=high` (fails the build on high/critical vulnerabilities) on every push/PR touching `backend/**`. Makes the exact manual verification sequence run at every step so far into an automatic, unskippable gate on every future change. Verified by simulating the identical sequence locally against a matching database (same env vars, same commands, same fresh `npm ci` install) before ever pushing, then confirmed the actual live run on GitHub (not just the local simulation) — all steps green in both. | Built, verified live on GitHub, confirmed |
| —    | **Full audit of Steps 01–05** ("ready for production" check): git state clean and fully pushed; no stray debug code/TODOs/hardcoded secrets found; no secrets ever committed to git history; migrated the database from absolute zero end-to-end; ran the full endpoint suite (all 9 cases: health, health/db, countries, register, duplicate-register 409, login, /me with/without token, 404) against a fresh migration; verified the **compiled production build** (not just `tsx` dev mode) end to end — plain JSON logs (no dev pretty-printing), security headers present, graceful SIGTERM shutdown, and fail-fast startup errors (exit 1, clear message) for a missing `DATABASE_URL`, missing `JWT_SECRET`, and a too-short `JWT_SECRET`. Found one real gap: `eslint@8.57.1` was EOL/deprecated upstream — migrated to ESLint 10 + flat config (`eslint.config.js`, `typescript-eslint` unified package), and proved the new config actually catches violations (not just "runs without error") by deliberately introducing an unused-variable violation and watching it get flagged before reverting. TypeScript deliberately stays on the stable 5.9.x line rather than the very new 7.0 rewrite — a documented choice, not an oversight. | Audited and confirmed production-ready |
| 06   | API contract formalization — replaced hand-rolled if/else request validation with real JSON Schema (Fastify's native AJV validator) on `POST /users`, plus response schemas across all routes (self-documenting, and fast-json-stringify strips any unlisted field before it can leak — e.g. a password hash could never reach a response even by accident). Added `@fastify/swagger` + `@fastify/swagger-ui` for live OpenAPI docs at `/docs`, off by default in production (`ENABLE_API_DOCS`). `POST /auth/login` deliberately keeps its body *unschemaed*, documented inline as to why: schema validation would return a different status/message than a wrong password, undermining the anti-enumeration property from Step 03. Found and fixed two real bugs, not hypotheticals: (1) `/auth/login`'s success response was missing `createdAt` against the shared user schema, which fast-json-stringify failed to serialize — a real 500 on every successful login, caught by testing the actual live flow end-to-end, not just validation cases. (2) A genuine **authentication-bypass race condition** in `app.authenticate`: its catch block called `reply.status(401).send(...)` without `return`-ing it; `reply.sent` is a getter over the raw response's `writableEnded`, an asynchronous I/O condition, so under timing-dependent conditions Fastify could invoke the protected route handler even after the 401 was "sent." Found via test flakiness, confirmed by reproducing it (the `/me` handler actually ran and hit the database with an unverified token's claims), and fixed structurally — not by adding `return` (which doesn't fully close the race) but by letting `jwtVerify()`'s rejection propagate as a thrown error, which Fastify routes through a completely separate, non-racy error branch that never reaches the route handler at all. Verified with a dedicated stress test (`tests/authenticate.test.ts`, 80 concurrent invalid-token requests, zero tolerance for any non-401) that reliably failed against the old code and passes against the new. Also caught and fixed a broken test-tampering method along the way: flipping a JWT's last base64url character doesn't reliably invalidate an HS256 signature (the last character of a 32-byte signature has 2 "don't-care" padding bits) — confirmed directly by decoding, fixed by corrupting a middle character instead. | Built, two real bugs found and fixed, stress-tested, confirmed |
| 07   | User profile management — completes user CRUD, which was a real gap: accounts could be created and logged into but never updated or removed. `PATCH /me` (partial update: email/displayName/countryId, reuses the same uniqueness/FK error handling as registration), `POST /me/password` (requires the current password before changing it — a stolen/shared-device token alone can't lock the real owner out), `DELETE /me` (requires the password — permanent hard delete, relevant for GDPR/CCPA-style right-to-erasure). Shared validation constants (email/password/displayName limits) extracted into `src/utils/userValidation.ts` so `POST /users` and the new `/me` routes can't quietly drift apart. Password-change and delete both rate-limited to 5/min as sensitive, abuse-prone actions. Verified live end-to-end against real Postgres, not just validation cases: profile update persists and is reflected on the next `GET /me`; password change actually changes which password logs in (old fails, new works) and is rejected with no change when the current password is wrong; email-uniqueness is enforced on update (two real accounts, second blocked from taking the first's email); account deletion actually removes the row (post-delete `GET /me` returns 404, post-delete login returns the same generic "Invalid email or password" as a never-registered email — no signal that the account used to exist) and is rejected with no change when the password is wrong. Also specifically checked the 204 (no-body) responses for a schema-mismatch crash, the exact class of bug found in Step 06 — verified clean. | Built, verified live end-to-end, confirmed |
| 08   | Token invalidation on password change — a real gap surfaced by Step 07's own work: JWTs are stateless, so changing a password did not revoke a token already issued (e.g. stolen via XSS or a shared device) - it stayed fully valid for up to its full 7-day life, defeating the entire point of a password change as a security response. Added `users.token_version` (migration `1700000003000_users_token_version`), embedded as `tv` in the JWT at login, bumped atomically in the same statement as the password update, and compared against the account's current value on every authenticated request (`app.authenticate`) - a mismatch is a `401`, thrown (not manually `reply.send()`'d) to stay on the same race-free error path established in Step 06. Two deliberate safety nets to avoid over-tightening: a token without a `tv` claim skips the check rather than failing closed (covers tokens signed directly in tests), and a `token_version` lookup for a since-deleted account is left to the route's own lookup to produce its usual `404`, not folded into this check as a `401`. Verified two ways: extensive live curl testing against a running server (old token works before the change, the exact same token used to make the password change is rejected immediately after, a fresh login's token keeps working, deletion still gets a clean 404) and a permanent automated regression test (`tests/tokenVersion.test.ts`) run against a real, migrated database rather than mocked - proved the test actually catches the bug by temporarily disabling the check and watching it fail before restoring it. Discovered along the way that CI's `DATABASE_URL` (set at the job level, migrated before tests run) already gives the test suite a real database to work with when a test needs one; set up the local equivalent (`caribbean_radio_hub_test`, migrated) so this could be verified locally before ever pushing, and documented the one-time local setup step in the README. | Built, real DB-backed regression test proven to catch the bug, confirmed |
| 09   | Password reset ("forgot password") flow — the single most glaring gap in the auth system: `POST /me/password` requires knowing the *current* password, so there was no way for a user to recover an account if they forgot it. Added `POST /auth/password-reset/request` and `POST /auth/password-reset/confirm`, with `password_reset_tokens` (migration `1700000004000`). Tokens are 256-bit random values (`crypto.randomBytes`), hashed with a fast SHA-256 (not bcrypt — that slowness defends against guessing a *low-entropy* secret, which doesn't apply to a 256-bit random value; a conscious tool choice, not a downgrade), single-use, expire after 1 hour, and requesting a new one invalidates any earlier unused one for the same account. `request` always returns the same response regardless of whether the email is registered; `confirm` always returns the same generic message for any invalid/expired/used/unknown token — same anti-enumeration posture as login. `confirm` reuses Step 08's `token_version` bump, so completing a reset also kills every session issued before it, not just the credential — the scenario this exists for (recovering a compromised account) would be undermined otherwise. No email provider is integrated yet, so the raw token is logged (clearly labeled as a placeholder) rather than emailed — documented prominently in the README as a real gap that must be closed with an actual send (SES/SendGrid/Postmark) before production launch, not silently left as-is. Verified live end-to-end against a running server (request → confirm → old password dead, new password works, pre-reset session token dead, token replay rejected, superseded token rejected while the newer one still works) and locked in with a permanent regression test (`tests/passwordReset.test.ts`) against a real, migrated database that spies on the logger to capture the token the same way a real email integration eventually would. | Built, verified live end-to-end and with a real-DB regression test, confirmed |
| —    | **Fix: Step 09's flagged gap — Fortune-500-grade password reset email delivery.** Replaced the "log the token" placeholder with a real, provider-agnostic transactional email pipeline: a **transactional outbox** (`email_outbox` table, migration `1700000005000`) where issuing the reset token and enqueueing its email now commit inside **one shared database transaction** (`createPasswordResetToken` was refactored to take the caller's transaction client instead of opening its own, the same pattern `enqueueEmail` already used) — a crash or error between the two can never leave a valid token with no email ever queued for it. A background worker (`src/email/outboxWorker.ts`), started and gracefully stopped alongside the HTTP server in `index.ts`, polls the outbox on an interval and sends independently per email, so a slow or down email provider can never make the request itself hang or fail; a failed send returns to `pending` for retry (capped at 5 attempts, then `failed` — a dead letter, never silently dropped). Claiming is done via a `FOR UPDATE SKIP LOCKED` CTE, so running more than one worker instance can never double-send — correct under future horizontal scaling from day one, not deferred. Delivery itself is SMTP-based (`nodemailer`), working with any real provider (SES, Postmark, SendGrid, Mailgun, Resend, or a corporate relay) via plain `SMTP_*`/`EMAIL_FROM` env vars — a provider swap is a config change, never new code; with no `SMTP_HOST` set, a dev-safe `ConsoleEmailProvider` is selected instead (loudly logged as such every time), so local dev/CI need no email setup while production cannot silently ship without one. Added a real HTML+text password-reset email template with the actual reset link (new `FRONTEND_URL` env var, required in production). Discovered and fixed two real cross-file test-isolation bugs while building this out, not hypotheticals: (1) an `afterAll(pool.end())` nested inside the wrong `describe` block closed the DB pool before a later sibling `describe` in the same file had run, breaking its DB calls; (2) once multiple test files could enqueue into and claim from the *same* live `email_outbox` table concurrently, assertions that assumed "this batch contains exactly the one email I just sent" became genuinely racy against other files' rows — fixed by matching on the specific recipient/subject a test cares about instead of the batch's total size, and by serializing test-file execution (`fileParallelism: false`) since these are integration tests against one shared, non-isolated real Postgres instance, not by papering over the race with timing hacks. Verified: full up/down/up migration cycle on both the dev and test databases; clean `tsc` build and `eslint` pass; the full test suite (66 tests across 13 files, including new `tests/emailOutbox.test.ts` and `tests/emailConfig.test.ts`, and a rewritten `tests/passwordReset.test.ts` that now captures the token through the real enqueue→claim→send pipeline instead of spying on a log line) run three consecutive times with zero flakes; `npm audit` clean; and a full live end-to-end run against a running compiled-equivalent dev server — register → request reset → background worker picks it up within one poll interval and "sends" it via `ConsoleEmailProvider` with the correct recipient/subject/reset-link body → confirm with that token succeeds → login with the new password succeeds → `SIGTERM` stops the worker and the server cleanly. | Built, real bugs found and fixed, verified live end-to-end, confirmed |
| 10   | API versioning (`/v1` prefix) — the last piece of the Foundation/Backend bucket that gets harder to add the longer it waits: every business-domain route (`/countries`, `/users`, `/auth/*`, `/me/*`) now lives under `/v1` (`src/app.ts` registers them inside a `{ prefix: "/v1" }` plugin), while `GET /health` and `GET /health/db` deliberately stay unversioned since they're infrastructure probes (load balancer/uptime monitor/orchestrator), not API contract surface a client depends on. This app has no shipped clients yet, so making the switch now costs a route prefix and nothing else; the Cross-Cutting Non-Negotiable that Android/iOS/Windows clients are all in scope means that once real installs exist in app stores, a given install can be stuck on whatever shape the API had when it was built for as long as that platform's review cycle takes — retrofitting versioning after that point would mean doing it under live traffic with old clients already depending on the unversioned shape. Documented the policy going forward in `backend/README.md` "API versioning": additive/backward-compatible changes ship straight into `/v1`, a genuine breaking change gets its own `/v2` plugin registered alongside `/v1` (never a mutation of `/v1` out from under whoever's still calling it). Mechanical but repo-wide: updated every test file's hardcoded route URLs (all 66 tests), the README endpoint list and prose route references, and the Swagger/OpenAPI config. Verified: clean `tsc` build and `eslint` pass; full 66-test suite passing three consecutive runs; `npm audit` clean; and a live-server check that the old unversioned paths now correctly 404 (`/countries`, `POST /users`) while `/v1/countries`, `/v1/users`, `/v1/auth/login` work end-to-end, `/health` and `/health/db` remain reachable unversioned, and the live-generated OpenAPI spec at `/docs/json` lists exactly the expected path set. | Built, verified live end-to-end, confirmed |
| 11   | User roles & admin authorization foundation — the last item in the Foundation/Backend bucket, and the one Radio Catalog curation, Events moderation, and the Admin Dashboard (Steps 12+, 24-30, 31-33) will all eventually need: doing it now, as a clean primitive, is cheaper than each of those retrofitting its own ad hoc protection later. Added `users.role` (migration `1700000006000_users_role`, `'user'`/`'admin'`, DB `CHECK` constraint), never client-settable (`POST /v1/users`'s existing `additionalProperties: false` schema silently strips a `role` field in the request body - verified, not assumed). The only path to `'admin'` right now is a config-driven `ADMIN_EMAILS` allowlist (`src/config/env.ts` `parseAdminEmails`, a pure function like `parseTrustProxy`/`parseSmtpConfig`) checked on both registration and every login (`ensureBootstrapAdminRole` in `usersRepository.ts`) - deliberately one-way (removing an email never demotes an existing admin, so an operator typo can't lock out the only admin) since there's no admin-management endpoint yet. `app.requireAdmin` (`src/app.ts`), used as `preHandler: [app.authenticate, app.requireAdmin]`, is the guard future admin routes will add; it deliberately looks the role up fresh from the database on every request rather than trusting a JWT claim - no `role` is embedded in the token at all, so a demotion (or promotion) takes effect on the very next request instead of whenever a stale token happens to expire or get reissued, the identical reasoning already applied to `token_version` in Step 08. Since no production route consumes `requireAdmin` yet (that's later Radio Catalog/Admin Dashboard work, not invented speculatively here), its guard behavior is proven against a real app instance and real database via a temporary test-only route registered in `tests/adminRole.test.ts` rather than a premature business endpoint. Found and fixed the identical cross-describe `afterAll(pool.end())` ordering bug from the Step 09 email-delivery fix while writing these tests - caught immediately this time from direct experience, not rediscovered the hard way. Verified: migration up/down/up on dev and test DBs; clean build and lint; the full suite (80 tests across 14 files, 14 new in `tests/adminRole.test.ts` covering `parseAdminEmails`, `ensureBootstrapAdminRole`'s promote/idempotent/never-demote behavior, and `app.requireAdmin`'s 401/403/200 cases plus same-token promotion-and-demotion-takes-effect-immediately) passing three consecutive runs; `npm audit` clean; and a live-server run confirming `ADMIN_EMAILS` bootstrap promotes on real registration and real login, a regular account stays `'user'`, a client-supplied `role: "admin"` is stripped, and `GET /v1/me` reflects the correct role. | Built, verified live end-to-end, confirmed |
| 12   | Radio Master Catalog core — the first step of the new bucket (Steps 12-18), and the foundational table everything else in it (search, stream reliability ranking, curation workflows) builds on. Added `radio_stations` (migration `1700000007000_radio_stations`: `country_id` FK to the 13 launch countries, `name`, `stream_url`, `website_url`, `description`, `is_active`, an audit-trail `created_by_user_id` SET-NULL-on-delete like `users.country_id`) and a full CRUD API under `/v1/stations` - public, unauthenticated `GET /v1/stations` (optional `?countryId=` filter) and `GET /v1/stations/:id`, with `POST`/`PATCH`/`DELETE` gated by Step 11's `[app.authenticate, app.requireAdmin]` - the catalog's first real consumer of that guard. Per the Project Standard (no rebroadcast/proxy/recording - a listener's device connects directly to a station's own stream), the table deliberately has no audio-storage column at all, only metadata and the URL itself. Three integrity guarantees enforced at the database/schema level, not just application code: `stream_url` is `UNIQUE` (a duplicate registration is a `409`, laying groundwork Step 16's data-quality work will build on rather than deferring the guarantee to then), `streamUrl`/`websiteUrl` must be HTTPS via a JSON Schema `pattern` (`format: "uri"` alone checks structure but not scheme), and `additionalProperties: false` strips any client-supplied field outside the documented body (same `removeAdditional: true` behavior already relied on for `POST /v1/users`). `PATCH { isActive: false }` is a soft-disable - pulls a station from the public listing and turns its detail route into the identical `404` an unknown id gets (never leaking "this exists but is hidden") while preserving the row for curation (Step 17); `DELETE` is a real hard delete reserved for genuine mistakes. Found one real bug while writing the validation tests, not a hypothetical: an unknown field in a `POST /v1/stations` body doesn't get rejected the way a first guess would suggest - Fastify's `removeAdditional: true` strips it before validation runs at all, so the request falls through to the auth check and correctly returns `401` for a token-less request rather than `400`; the test was wrong, not the code, and got fixed to assert the real (and correct) documented behavior instead, with a companion real-DB test proving the field is silently stripped rather than persisted. Verified: migration up/down/up on dev and test DBs; clean build and lint; the full suite (96 tests across 16 files, 16 new across `tests/stations.validation.test.ts` and `tests/stations.test.ts`) passing three consecutive runs; `npm audit` clean; and a full live-server run - create as admin, public list/detail, `401` with no token, `403` for a non-admin token, HTTPS-only enforcement rejecting a plain-`http://` stream URL, deactivate removing it from public view, delete, and the live-generated OpenAPI spec listing the new `/v1/stations` and `/v1/stations/{id}` paths. | Built, real bug found and fixed, verified live end-to-end, confirmed |
| 13   | Station metadata taxonomy — genres and languages. Added `genres`/`languages` as database-driven lookup tables (migration `1700000008000_genres_and_languages`, seeded by a separate `1700000008500_seed_genres_and_languages` - deliberately two migrations, not one: mixing `pgm.createTable` with an immediate `pgm.db.query` seed insert in the same migration is the exact bug already hit and fixed once for countries in Step 02, so this time the split was applied from the start rather than discovered again the hard way) - 12 Caribbean-radio genres and the 4 languages actually spoken on-air across the 13 launch countries (English, Spanish, French, French Creole), publicly listed at `GET /v1/genres`/`GET /v1/languages`. Many-to-many via `station_genres`/`station_languages` junction tables (composite primary key, `ON DELETE CASCADE` both directions) since a station is rarely one genre and several launch countries are functionally bilingual on-air. `POST`/`PATCH /v1/stations` gained optional `genreIds`/`languageIds`: a write *replaces* the full tag set rather than merging (simpler and unambiguous, matches the low write volume an admin-curated catalog actually sees) - omitted leaves existing tags untouched, `[]` clears them, an unknown id is a `400` resolved via the failing foreign key's constraint name so the error names which one (genre vs. language vs. country) actually failed. Reads use two independent correlated `json_agg` subqueries rather than joining both junction tables into the main query directly - joining two separate one-to-many relations at once would fan a station out into a duplicated row per genre-times-language combination before any aggregation could run; `COALESCE(..., '[]')` keeps "no tags" an empty array, never a `null` clients have to guard against. Verified: migration up/down/up on dev and test DBs (including confirming re-seeding after a down/up cycle lands exactly 12 genres and 4 languages, not duplicates); clean build and lint; the full suite (104 tests across 17 files, 8 new covering public listing, hydrated (not just echoed-id) genre/language objects on create and read, full-set replacement, untouched-when-omitted and cleared-with-`[]` on update, `400` for an unknown genre/language id, a duplicate id within one request tolerated rather than erroring, and a deleted station's junction rows confirmed gone via a direct query) passing three consecutive runs; `npm audit` clean; and a live-server run tagging a real station with two genres and a language, replacing its tags via `PATCH`, and confirming an unknown genre id returns the correct `400` message. | Built, verified live end-to-end, confirmed |
| —    | **Adversarial security pass** — in response to a standing directive to build and defend as if a skilled, motivated attacker is actively trying to break in (now codified above as a permanent Cross-Cutting Non-Negotiable), directly attacked the live running server across everything built in Steps 01-13, not just reviewed it: JWT forgery (`alg: none`, invalid/nonsense `alg` headers, weak-secret signature guessing, oversized-header DoS shape), auth/authz bypass (missing tokens, cross-role privilege escalation attempts, mass-assignment of `role` via `PATCH /v1/me`), SQL injection through every input class (email, station name, query params, path params - including a literal `'); DROP TABLE radio_stations;--` payload, round-tripped as inert stored data and verified against the real table afterward), rate-limit brute-forcing (confirmed `429` under an actual burst, not just configured), CORS/security-header inspection, and error-response leakage (stack traces, `passwordHash`, internal detail) on 404s and malformed bodies. One real, non-hypothetical finding: a forged token with an invalid `alg` header returned `500` instead of `401` - `@fastify/jwt` only wraps 4 of `fast-jwt`'s ~15 verification-failure codes into a proper-401 `FastifyError`, so an invalid-algorithm forgery attempt (the classic `alg: none` technique) fell through the app's generic error handler to its `500` default, misclassifying a hostile/malformed request as the server's own bug. Never an auth bypass (the token was still correctly rejected either way) but a real gap, found live, not in review. Fixed in `app.ts`'s `setErrorHandler` by recognizing any `FAST_JWT_*` error code as `401` regardless of whether the library attached a `statusCode`, and additionally hardened by explicitly pinning the JWT algorithm to HS256 in both `sign` and `verify` (was implicitly accepting any of HS256/HS384/HS512 for a plain secret key) - standard JWT defense-in-depth per OWASP's JWT cheat sheet, not closing an active hole (forging any HMAC variant still requires `JWT_SECRET` either way) but removing reliance on a library default. Locked in with 3 new regression tests in `tests/authenticate.test.ts`, proven to fail against the pre-fix code the same way every other regression fix in this build has been proven: reverted the fix, watched the new tests fail with the exact `500`, restored it, watched them pass. Verified: clean build and lint; full 107-test suite passing three consecutive runs; `npm audit` clean. | Real vulnerability class found via live adversarial testing, fixed, hardened further, regression-tested, confirmed |
| 14   | Radio station search, filtering, and pagination. `GET /v1/stations` gained `genreId`/`languageId` filters (each an `EXISTS` subquery against the relevant junction table, avoiding the row-fan-out a direct `JOIN` on two separate one-to-many relations would cause - the identical reasoning already applied to the genres/languages `json_agg` subqueries in Step 13) and `q`, a case-insensitive `ILIKE` substring match against the station name (plain `ILIKE`, not a full-text index - appropriate for a curated catalog of dozens to a few hundred stations, not premature complexity for a corpus this system doesn't have). All filters combine with `AND`. The listing is now genuinely paginated, not an unbounded "return everything" that would only become a real problem once the catalog grows: `limit` (default 50, capped at 100) and `offset`, enforced both in the route's JSON Schema and again inside `listStations` itself so a future internal caller reaching the repository directly can't bypass the cap by skipping the schema. `pagination.total` is computed via `COUNT(*) OVER()` in the same query as the page of results, not a separate `COUNT(*)` round-trip whose `WHERE` clause could drift out of sync with the actual filter. A caller-supplied search term is escaped (`%`, `_`, `\`) before being bound as the `ILIKE` pattern, so a literal percent sign or underscore in a search term can't be misread as a SQL wildcard - never a SQL-injection concern either way (a bound parameter, not string concatenation), purely about search results matching what was actually typed, and directly exercised as its own adversarial-style test (a search for `<token>%` correctly matches nothing). Verified: clean build and lint; full 116-test suite (9 new, split between real-DB behavior in `tests/stations.test.ts` and schema-rejection cases in `tests/stations.validation.test.ts`) passing three consecutive runs; `npm audit` clean; and a live-server run seeding three real stations, walking a two-page pagination sequence with a stable `total` throughout, filtering by genre with an `EXPLAIN`-verified query plan (confirmed the planner actually uses the `station_genres_genre_id_index` added in Step 13, not a full scan of the junction table), and confirming the literal-`%` escape live. | Built, verified live end-to-end, confirmed |
| —    | **Standing-instructions audit — string input hygiene.** In response to a request to review every standing instruction given throughout this build and confirm each is still being actively followed, re-audited the codebase against an earlier one ("automatically cleaning all strings") that had never been given its own explicit Cross-Cutting Non-Negotiable line - now added above. The audit found a real, concrete gap while checking: `src/routes/stations.ts`'s `POST`/`PATCH` handlers never trimmed leading/trailing whitespace from `name`/`description`/`streamUrl`/`websiteUrl`, unlike `POST /v1/users`/`PATCH /v1/me`, which have trimmed `email`/`displayName` via a `preValidation` hook since Step 02 - the hygiene pattern was simply never carried over when the stations routes were built in Step 12. Fixed by adding the identical `preValidation` trim hook to both routes. Concretely: this closes a real correctness bug, not just a cosmetic one - a genuinely valid `streamUrl` with copy-pasted surrounding whitespace was being rejected outright by the HTTPS `pattern` check (`^https://` anchored against untrimmed leading spaces), and an all-whitespace `name` was passing validation and creating a blank-looking station instead of failing its `minLength: 1` check. Locked in with 3 new regression tests in `tests/stations.test.ts`, proven to fail against the pre-fix code the same way every other regression fix in this build has been proven: reverted the fix, watched all three fail (including the exact "valid URL rejected" scenario), restored it, watched them pass. A background audit of every remaining route (`users.ts`, `auth.ts`, `me.ts`, `stations.ts`, the read-only list routes) for the same class of gap turned up one more, softer case: `POST /v1/auth/password-reset/confirm`'s `token` was compared untrimmed against an exact SHA-256 hash - never a data-corruption risk (the token is always lowercase hex, so whitespace could never be a legitimate part of a real one), but a genuinely valid token picking up incidental whitespace on a copy/paste path would silently look invalid instead of completing the reset. Fixed with the same `preValidation` trim pattern, deliberately *not* applied to `newPassword` in the same handler (or any password field anywhere in the app) - a password's leading/trailing characters can be part of what the user actually meant to type, so trimming one would silently change a user's password to something they didn't enter. Every password field across the app was confirmed to correctly remain untrimmed; the `q` search param on `GET /v1/stations` was confirmed already-correct (trimmed defensively inside `stationsRepository.ts` itself, protecting any future direct caller, not just the HTTP route - the same defense-in-depth reasoning already applied to that function's `limit`/`offset` capping). Locked in with a fourth regression test in `tests/passwordReset.test.ts`, proven the same way. Verified: clean build and lint; full 120-test suite passing three consecutive runs; `npm audit` clean; and two live-server runs - one confirming a padded name/description/streamUrl now saves trimmed and an all-whitespace name correctly returns `400`, a second confirming a real password-reset token wrapped in whitespace still completes the reset and the new password works. | Real correctness bugs found via standing-instruction audit, fixed, regression-tested, confirmed |
| 15   | Admin station listing (`GET /v1/admin/stations`) - closes a real gap explicitly flagged in Steps 12 and 14's own code comments ("admin tooling that needs to see everything queries the repository directly... or gets its own route"): before this, an admin had no way through the API to see a curated-off (inactive) station at all, browse what had been deactivated, or find a station that mysteriously stopped appearing publicly - only a direct database query could. Refactored `stationsRepository.ts`'s filter from a plain `activeOnly` boolean (which could only ever mean "active-only" or "no filter," never "inactive-only") into a genuine tri-state `isActive` (`undefined` = no filter, `true`/`false` = exactly that state) - `is_active` is now bound as a real query parameter in the generated SQL too, closing a small inconsistency where it had been a hardcoded `true` literal instead of matching every other filter's parameterized style. `GET /v1/stations` still always passes `true` explicitly and is structurally incapable of exposing the new filter to a client (it's a different route, not a query param the public endpoint accepts); `GET /v1/admin/stations` (admin-gated via Step 11's `requireAdmin`) is what exposes it for real, reusing the exact same `countryId`/`genreId`/`languageId`/`q`/pagination support the public route already has. Verified: clean build and lint; full 125-test suite (5 new) passing three consecutive runs; `npm audit` clean; and a live-server run creating a station, deactivating it, confirming it vanishes from the public listing, and confirming the admin route finds it (and correctly narrows to it or excludes it via `isActive=false`/`isActive=true`). Also ran a real dependency-license audit while addressing the standing commercial-license-safety requirement (see Cross-Cutting Non-Negotiables): every third-party package in the tree resolves to a commercial-safe license (MIT/ISC/Apache-2.0/BSD-2/3-Clause/BlueOak-1.0.0/MPL-2.0); the one `UNLICENSED` entry was confirmed to be this project's own first-party package (correct for proprietary product code) and made explicit in `package.json` rather than left implicit. | Built, real gap closed, verified live end-to-end, confirmed |
| 16   | Radio station data quality — near-duplicate stream URL detection, layered on top of Step 12's exact-match `stream_url` UNIQUE constraint (which only ever caught a byte-for-byte repeat). Two admin-typed URLs that are the same stream in every way that matters - `https://Stream.Example.com/live` vs. `https://stream.example.com/live`, or a bare `https://stream.example.com` vs. the same URL with a trailing slash - would both pass that exact-match check and silently create two catalog rows for one station. Added `normalizeStreamUrl` (`src/utils/streamUrlValidation.ts`): case-folds scheme and host (case-insensitive per RFC 3986 §3.1/§3.2.2, verified against the RFC rather than assumed), folds an explicit default `:443` port away, and folds a trailing slash on the *root* path only - a longer path's trailing slash is deliberately left alone, since real Icecast/Shoutcast-style stream servers can route `/stream` and `/stream/` to different mount points, so folding that would be a false positive. Stored in a new `stream_url_normalized` column with its own database-level UNIQUE constraint (migrations `1700000009000`/`1700000009500`, split into two files for the identical `pgm.addColumn`-is-deferred-DSL-vs-`pgm.db.query`-is-immediate reason already hit and fixed for Step 02's countries and Step 13's genre/language seed - recognized immediately this time, not rediscovered the hard way), reported as a distinct `NearDuplicateStreamUrlError` (409) so it's never confused with the original exact-match message. Verified: migration up/down/up on dev and test DBs; clean build and lint; the full 139-test suite (12 new, split between a pure-unit `tests/streamUrlValidation.test.ts` and real-DB behavior in `tests/stations.test.ts`) passing three consecutive runs; `npm audit` clean; proven to actually catch the bug (not just pass by construction) by temporarily writing the raw, non-normalized `streamUrl` into the new column and watching the three new near-duplicate tests fail with the exact wrong status code (`201`/`200` instead of `409`) before restoring the fix; and a live-server run confirming a host-case variant, a root-path trailing-slash variant, and a genuine byte-for-byte repeat all get the correct, correctly-differentiated `409` responses. | Built, real gap closed, verified live end-to-end, confirmed |
| —    | **License-safety verification (real research, not recall)** — in direct response to a standing instruction that the commercial-license-safety requirement is an industry/legal requirement, not a personal preference, and that any code/licensing fact I'm not certain of must be verified via real search rather than asserted from memory: Step 15's dependency-license audit had labeled MPL-2.0 (3 packages) and BlueOak-1.0.0 (5 packages) "commercial-safe" from recollection. Verified both against authoritative current sources rather than letting that stand on recall alone. **BlueOak-1.0.0**: confirmed via the Blue Oak Council's own license FAQ (blueoakcouncil.org/license-faq) and ChooseALicense.com — a permissive license (peer to MIT/BSD/Apache-2.0, deliberately written to be clearer and more legally complete than MIT/BSD/ISC) with an explicit patent grant; licensed works, modifications, and larger works "may be distributed under different terms and without source code" — no obligation of any kind flows back to a codebase that merely depends on a BlueOak-licensed package. Confirmed genuinely commercial-safe as used here (a dependency, never modified/redistributed on its own). **MPL-2.0**: confirmed via the Mozilla Foundation's own MPL-2.0 FAQ (mozilla.org/MPL/2.0/FAQ) and corroborating sources — copyleft, but **file-level**, not project-level: it explicitly permits combining MPL-licensed files with proprietary code and statically linking/distributing the combination as part of a larger proprietary work, unlike GPL/AGPL-style copyleft. The obligation that exists is narrow and specific — if any of the *original MPL-licensed files themselves* are modified and distributed, those modified files must be released under MPL-2.0; new proprietary files placed alongside them are never affected. As used here, these 3 packages are consumed as unmodified npm dependencies (never forked/edited and redistributed), so this codebase carries no MPL disclosure obligation — genuinely commercial-safe under that condition, which is now recorded explicitly (not just asserted) as the reason: **never modify-and-redistribute an MPL-licensed dependency's own files without first re-checking this obligation.** No correction was needed to Step 15's underlying conclusion, but the original entry stated it from memory; this entry replaces that with a sourced verification, and the same discipline (search when uncertain, cite the source, don't rely on recall for anything with legal weight) now applies to every future step. Sources: Mozilla, "MPL 2.0 FAQ" (mozilla.org/en-US/MPL/2.0/FAQ/); Blue Oak Council, "Model License FAQ" (blueoakcouncil.org/license-faq); ChooseALicense.com, "Blue Oak Model License 1.0.0" (choosealicense.com/licenses/blueoak-1.0.0/). | Verified via real research (not recall), sourced, confirmed |
