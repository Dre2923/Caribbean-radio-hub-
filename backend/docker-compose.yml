services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: caribbean
      POSTGRES_PASSWORD: caribbean
      POSTGRES_DB: caribbean_radio_hub
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U caribbean"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
